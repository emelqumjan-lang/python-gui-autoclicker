# python-gui-autoclicker


# Modern Python Autoclicker 🚀

A fast, fully responsive, and beautifully designed Python autoclicker with a modern GUI. Thanks to multi-threading architecture, the interface never freezes or hangs during intensive clicking.

## ✨ Features
* **Modern UI:** Sleek dark-themed interface styled after Windows 11 using `CustomTkinter`.
* **Multi-threaded (No Freezes):** The clicking loop runs entirely in a background thread, keeping the GUI 100% responsive.
* **Global Hotkeys:** Start and stop clicking instantly using a hotkey (default is `F6`), even when you are in-game or the app is minimized.
* **Persistent Settings:** Automatically saves and loads your interval preferences and button configurations via `config.json`.

## 🛠️ Installation & Setup

1. Make sure you have **Python 3.8+** installed.
2. Clone this repository or download the source code.
3. Install the required dependencies:
```bash
   pip install -r requirements.txt




```markdown
# Современный Автокликер на Python 🚀

Быстрый, отзывчивый и красивый автокликер на Python с современным графическим интерфейсом. Благодаря многопоточной архитектуре, окно программы никогда не зависает и не лагает во время активных кликов.

## ✨ Особенности
* **Современный GUI:** Стильный интерфейс в темных тонах в духе Windows 11 на базе библиотеки `CustomTkinter`.
* **Многопоточность (Без зависаний):** Цикл кликов работает в отдельном фоновом потоке, поэтому интерфейс всегда остается на 100% активным.
* **Глобальные хоткеи:** Запуск и остановка по нажатию горячей клавиши (по умолчанию `F6`), даже если игра или браузер развернуты на весь экран, а кликер свернут.
* **Сохранение настроек:** Программа автоматически сохраняет ваши задержки и выбранные кнопки мыши в файл `config.json`.

## 🛠️ Установка и настройка

1. Убедитесь, что у вас установлен **Python 3.8** или выше.
2. Склонируйте репозиторий или скачайте архив с кодом.
3. Установите необходимые библиотеки одной командой:
```bash
   pip install -r requirements.txt
